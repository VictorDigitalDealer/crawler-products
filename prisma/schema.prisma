datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Product {
  id          Int      @id @default(autoincrement())
  name        String
  shop        String
  url         String   @unique
  price       Float
  category    String
  categoryUrl String
  scrapedAt   DateTime @default(now())
}
